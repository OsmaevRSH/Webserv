server {
	route {
		autoindex on;
		root ./conf;
		max_body_size 55;
		index ./conf;
		route {
        		autoindex on;
        		root ./conf;
        		max_body_size 100;
        		index ./conf;
        	}
	}
}

